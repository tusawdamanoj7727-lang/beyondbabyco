"use client";

/**
 * Canonical storefront product card — re-exports catalog ProductCard.
 * Prefer importing from `@/components/catalog/ProductCard`.
 */
export { default, default as ProductCard } from "@/components/catalog/ProductCard";
