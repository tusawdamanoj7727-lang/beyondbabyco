# Copy to production.env (gitignored) for local production E2E runs.
# Customer credentials — required for checkout, account, COD, and Razorpay specs.
E2E_CUSTOMER_EMAIL=
E2E_CUSTOMER_PASSWORD=

# Admin credentials — set via shell env only; never commit real values.
# E2E_ADMIN_EMAIL=
# E2E_ADMIN_PASSWORD=
